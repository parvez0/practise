const ngrok = require("./index.js");

const url = ngrok
  .connect({
    subdomain: "test",
    port: 8080
  })
  .then(data => {
    console.log("url: ", data);
  })
  .catch(err => {
    console.log(err);
  });
