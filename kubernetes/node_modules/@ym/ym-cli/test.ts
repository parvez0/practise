import {serve, App} from '@ym/bot-engine';
import * as path from 'path';
import * as process from 'process';const CURRENT_PATH = process.cwd();serve(require(path.join(CURRENT_PATH, '/dist/functions/index')), {uid: process.env.uid, bot: process.env.botId});
