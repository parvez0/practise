import {serve, App} from './index';
const uuid = require('uuid/v1');

serve({
    main: (app: App, context: object) => {
        console.log("Hello ... Im here");
        return app.start();
    }
}, {
    uid: uuid()
});
